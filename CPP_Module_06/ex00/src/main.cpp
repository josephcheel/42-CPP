#include <iostream>
#include <string>

bool	isnum(std::string str)
{
	size_t lenght = str.length();
}

int	main(int ac, char **av)
{
	std::string nbr;

	if (ac <= 1)
		return 0;
	nbr = av[1];
	if (nbr[nbr.length() - 1] == 'f')
	{
	
	}

		
}
